import styled from "styled-components";

export const StyledComponent = styled.div`
  .ant-btn-icon-only {
    width: auto;
    height: auto;
    line-height: 1;
    border: none;
    background: none;
    padding: 0;
}
`;
 