import styled from "styled-components";

export const StyledComponent = styled.div`
.ant-btn-icon-only {
    width: auto;
    padding: 0;
    border: none;
    background: none;
}
.isCanChange {
    cursor: pointer;
}
`;
